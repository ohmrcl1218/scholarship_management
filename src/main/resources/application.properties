# Server Configuration - Railway uses PORT environment variable
server.port=${PORT:9090}
server.servlet.context-path=/api

# Railway MySQL connection
spring.datasource.url=${DATABASE_URL:jdbc:mysql://shuttle.proxy.rlwy.net:51030/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:urQVGztFWiWTVToWXmqEdluRQFxPzJSO}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hikari Connection Pool Settings
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-test-query=SELECT 1

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Configuration
jwt.secret=${JWT_SECRET:hirayaFoundationSecretKey2026SecureJWTTokenForScholarshipSystem}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Application Settings
app.frontend-url=${FRONTEND_URL:http://localhost:3000}
app.base-url=${BASE_URL:http://localhost:9090}

# Security
security.enable-csrf=false

# Logging
logging.level.com.example.scholarship=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# CORS will be handled by CorsConfig.java